/* 
This middleware identifies the user,
 checks if the user is authenticated.
    compares the hash of the user's transaction pin
    if the hash passes, transfers should be authorized 
    else transfer cannot be authorized.
*/
